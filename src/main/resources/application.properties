spring.application.name=Employee Performance Monitoring

# Spring Boot 4.0 renamed this from spring.data.mongodb.uri to spring.mongodb.uri
spring.mongodb.uri=${MONGO_URI}

# still under the old prefix in Boot 4 (needs Spring Data MongoDB)
spring.data.mongodb.auto-index-creation=false

server.servlet.context-path=/
server.port=${PORT:8080}
ai.api.key=${AI_API_KEY:test123}
